// src/lib/tooltip/displayValuesFor.js
// This module provides a function to extract the current item from moreProps for tooltip display.
// displayValuesFor.js

export default function displayValuesFor(props, moreProps) {
	const { currentItem } = moreProps;
	return currentItem;
}
